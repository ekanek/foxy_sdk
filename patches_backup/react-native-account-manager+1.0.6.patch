diff --git a/node_modules/react-native-account-manager/android/build.gradle b/node_modules/react-native-account-manager/android/build.gradle
index da16bc7..01d272f 100644
--- a/node_modules/react-native-account-manager/android/build.gradle
+++ b/node_modules/react-native-account-manager/android/build.gradle
@@ -1,6 +1,10 @@
 buildscript {
     repositories {
-        jcenter()
+        mavenCentral()
+        google()
+        maven {
+            url "https://maven.google.com"
+        }
     }
 
     dependencies {
@@ -11,12 +15,12 @@ buildscript {
 apply plugin: 'com.android.library'
 
 android {
-    compileSdkVersion 23
-    buildToolsVersion "23.0.1"
+    compileSdkVersion 28
+    buildToolsVersion "28.0.3"
 
     defaultConfig {
         minSdkVersion 16
-        targetSdkVersion 22
+        targetSdkVersion 28
         versionCode 1
         versionName "1.0"
     }
@@ -27,8 +31,12 @@ android {
 
 repositories {
     mavenCentral()
+    google()
+    maven {
+        url "https://maven.google.com"
+    }
 }
 
 dependencies {
-    compile 'com.facebook.react:react-native:+' // from node_modules
+    implementation 'com.facebook.react:react-native:+' // from node_modules
 }
